[flake8]
# Match the black formatter's line length (88)
max-line-length = 88
exclude =
    # No need to traverse our git directory
    .git,
    # There's no value in checking cache directories
    __pycache__,
per-file-ignores =
    # imported but unused
    __init__.py: F401